/*

import { Context } from '../../utils/types';
import { handleError } from './../../utils/common';

const ABC = (_parent : any, args : any, ctx: Context, _info : any) => {

    const { prisma , user } = ctx;
    const { } = args.input;
    try {
        
    } catch (error) {
       handleError(error) 
    }
}

export default {
    ABC
}

*/
